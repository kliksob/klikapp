const stream = require("mithril/stream");

module.exports = {
  head: {
    title: stream("Kliksob.com"),
    description: stream(
      "KlikApp Web Framework, Simple Isomorphic web app with mithrilJS"
    )
  }
};
